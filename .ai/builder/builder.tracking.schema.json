{
  "project-name": "",
  "plans": [
    {
      "slug": "{{container | slug}}-{{feature | slug}}",
      "plan": "/containers/{{container | slug}}/docs/{{feature | slug}}.plan.md",
      "status": "{{: planned | implemented | pending  }}",
      "tasks":[
        {
          "id": "{{#}}",
          "slug": "{{container | slug}}-{{feature | slug}}-{{task | slug}}",
          "task": "/containers/{{container | slug}}/.ai/tasks/{{task | slug}}.task.md",
          "status": "{{: planned | prompted | implemented | pending }}",
          "git": "{{ git id for the task commit }}"
        }
      ]
    }
  ]
}