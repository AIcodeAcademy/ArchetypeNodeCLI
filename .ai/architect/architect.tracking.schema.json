{
  "project-name": "",
  "features": [
    {
      "id": "#",
      "name": "{{ name | short sentence | Title Case }}",
      "slug": "f{{ id }}-{{ name | slugify }}",
      "blueprint": "/docs/{{slug}}.blueprint.md",
      "status": "{{: draft | defined | planned | pending | implemented | failed }}"
    }
  ],
  "containers": [
    {
      "id": "#",
      "archetype": "{{: node-cli | express-api | angular-spa | other }}",
      "slug": "c{{ id }}-{{ archetype | slugify }}"
    }
  ]
}