{
	"project-name": "",
	"features": [
		{
			"id": "f{{#}}",
			"name": "{{ name | short sentence | Title Case }}",
			"slug": "{{ id }}-{{ name | slugify }}",
			"status": "{{: draft | defined }}"
		}
	],
	"containers": [
		{
			"id": "c{{#}}",
			"archetype": "{{: node-cli | express-api | angular-spa | other }}",
			"slug": "{{ id }}-{{ archetype | slugify }}",
			"status": "{{: draft | defined }}"
		}
	]
}
